const express=require("express"),cors=require("cors"),bbsRoute=require("./route/bbs"),loginRoute=require("./route/login"),path=require("path"),moment=require("moment");require("moment-timezone"),moment.tz.setDefault("Asia/Seoul");const date=moment().format("YYYY-MM-DD HH:mm:ss");console.log(date);const app=express();app.use(cors()),app.use(express.json()),app.use("/",bbsRoute),app.use("/",loginRoute),app.use(express.static(path.join(__dirname,"public")));const port=5e3;app.listen(5e3,()=>{console.log("Example app listening at http://localhost:5000")});